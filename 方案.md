下面给你一版**最终方案**，只讲怎么做，不做对比。

# 方案名称

**基于共享 Transformer 的多策略 Alpha 因子强化学习生成方法**

核心思路：

> 使用一个共享 Transformer 编码表达式状态，在其后接多个策略 head。不同策略 head 负责不同表达式搜索偏好；其中探索 head 引入基于 AST 结构访问次数的内在奖励。所有策略生成的候选因子统一进入 AlphaPool 评价和筛选。

------

# 一、系统总体结构

```text
表达式 token 序列
        ↓
Token Embedding
        ↓
TransformerSharedNet
        ↓
共享状态表示 h
        ↓
┌──────────────┬──────────────┬──────────────┬──────────────┐
base head      simple head    ts head        explore head
收益导向        简单表达式      时序结构偏好    结构探索
        ↓              ↓              ↓              ↓
候选 Alpha 表达式生成
        ↓
合法性检查 + 去重
        ↓
统一 AlphaPool 评价
        ↓
最终因子池
        ↓
IC / RankIC / 结构指标 / 可视化分析
```

------

# 二、四个策略 head 设计

## 1. base head：收益导向策略

目标：生成以组合边际收益为主要目标的候选表达式。

设置：

```text
Reward = RE
Temperature = 1.0
无额外 token bias
```

作用：作为主要收益导向策略，负责生成正常 Alpha 候选。

------

## 2. simple head：简单表达式策略

目标：生成较短、较浅、可解释性更强的表达式。

实现方式：

```text
Reward = RE
max_nodes = 8 或 10
max_depth = 3 或 4
复杂算子 logits 降权
```

复杂算子可以包括：

```text
Corr, Cov, TsRank, 多层嵌套类算子
```

动作采样时：

```text
logits[complex_tokens] -= λ_simple
```

推荐：

```text
λ_simple = 0.3 ~ 0.5
```

------

## 3. ts head：时序算子策略

目标：偏向生成时间序列类 Alpha 表达式。

重点鼓励的 token：

```text
Delay
Delta
Mean
Std
Corr
TsRank
```

实现方式：

```text
Reward = RE
对时序类算子 logits 加正偏置
```

动作采样时：

```text
logits[time_series_tokens] += λ_ts
```

推荐：

```text
λ_ts = 0.4 ~ 0.6
```

------

## 4. explore head：结构探索策略

目标：探索较少出现的表达式结构。

使用内在奖励：

```text
Reward = RE + β R_int
```

其中：

```text
R_int = 1 / sqrt(N(AST_cluster) + 1)
```

含义：

- `AST_cluster` 表示表达式结构簇；
- `N(AST_cluster)` 表示该结构簇已经出现的次数；
- 结构越少见，内在奖励越高；
- 出现次数越多，内在奖励越低。

推荐：

```text
β = 0.05 ~ 0.2
```

------

# 三、策略 head 的动作分布

对第 `k` 个策略 head，动作概率定义为：

```text
π_k(a | s) = softmax((logits_k + bias_k) / T_k)
```

其中：

| 符号       | 含义                               |
| ---------- | ---------------------------------- |
| `logits_k` | 第 k 个策略 head 输出的动作 logits |
| `bias_k`   | 该策略的 token 偏置                |
| `T_k`      | 采样温度                           |
| `π_k(a     | s)`                                |

四个 head 的推荐设置：

| head    | Temperature | Token bias         | Reward      |
| ------- | ----------- | ------------------ | ----------- |
| base    | 1.0         | 无                 | RE          |
| simple  | 0.9         | 复杂算子降权       | RE          |
| ts      | 1.0         | 时序算子加权       | RE          |
| explore | 1.3         | 可无或轻微结构偏置 | RE + βR_int |

------

# 四、训练流程

每个 episode 开始时选择一个 head。

推荐使用轮询方式：

```text
episode 1：base head
episode 2：simple head
episode 3：ts head
episode 4：explore head
episode 5：base head
...
```

训练流程：

```text
1. 选择当前 episode 使用的策略 head
2. 输入当前表达式 token 序列
3. TransformerSharedNet 编码状态
4. 当前 head 输出动作 logits
5. 应用动作 mask，屏蔽非法动作
6. 根据 head 类型加入 token bias 和 temperature
7. 采样动作，继续生成表达式
8. 表达式完成后计算 RE
9. 如果是 explore head，额外计算 R_int
10. 使用 PPO 更新共享 Transformer 和当前 head
```

------

# 五、模型结构建议

推荐使用：

```text
共享 TransformerSharedNet
+ 四个 actor head
+ 四个 value head
```

结构如下：

```text
TransformerSharedNet
        ↓
┌────────────────┬────────────────┬────────────────┬────────────────┐
base actor       simple actor      ts actor          explore actor
base value       simple value      ts value          explore value
```

这样每个策略都有自己的 actor 和 critic，训练逻辑更清晰。

如果想降低实现复杂度，也可以共享 value head，但论文上多 value head 更好解释。

------

# 六、内在奖励模块设计

## 1. AST 结构签名

对每个表达式提取结构签名，只保留结构，不保留具体字段。

例如：

```text
Rank(Mean(close, 10))
```

可以抽象为：

```text
Rank(Mean(FIELD, CONST))
```

再例如：

```text
Corr(Rank(volume), Rank(close), 20)
```

可以抽象为：

```text
Corr(Rank(FIELD), Rank(FIELD), CONST)
```

这样可以统计表达式结构，而不是只统计字符串。

------

## 2. 结构访问计数

维护一个字典：

```python
ast_count = {
    "Rank(Mean(FIELD, CONST))": 12,
    "Corr(Rank(FIELD), Rank(FIELD), CONST)": 5,
}
```

每生成一个合法表达式：

```python
cluster = get_ast_signature(expr)
n = ast_count[cluster]
r_int = 1 / sqrt(n + 1)
ast_count[cluster] += 1
```

------

## 3. 内在奖励归一化

为了避免 `R_int` 过大或过小，建议做归一化：

```text
R_int_norm = (R_int - mean(R_int)) / (std(R_int) + ε)
```

最终：

```text
R_explore = RE + β R_int_norm
```

如果实现时间紧，也可以先不归一化，直接使用较小的 `β`。

------

# 七、候选因子统一筛选

所有 head 生成的候选表达式都进入统一 AlphaPool。

每个候选保留来源信息：

```json
{
  "expr": "Rank(Mean(close, 10))",
  "source_head": "ts",
  "RE": 0.012,
  "ast_cluster": "Rank(Mean(FIELD, CONST))",
  "node_count": 5,
  "depth": 3
}
```

AlphaPool 负责：

```text
1. 计算候选因子值
2. 计算加入因子池后的边际收益 RE
3. 判断是否接收
4. 更新因子池
5. 记录该候选来自哪个 head
```

------

# 八、实验分组

建议设置 4 组核心实验。

| 实验组                | 说明                           |
| --------------------- | ------------------------------ |
| Single-Transformer    | 单策略 Transformer + RE        |
| MultiHead             | 多策略 head，不加内在奖励      |
| MultiHead + Intrinsic | 多策略 head + explore 内在奖励 |
| Random-valid          | 合法随机表达式生成基线，可选   |

如果时间够，可以再加：

| 实验组           | 说明                     |
| ---------------- | ------------------------ |
| LSTM-single      | LSTM 单策略对照          |
| Transformer-base | Transformer 单 head 对照 |

------

# 九、评价指标

评价分成两类：收益指标和探索指标。

## 1. 收益指标

| 指标          | 含义                       |
| ------------- | -------------------------- |
| IC            | 因子值与未来收益的线性相关 |
| RankIC        | 横截面排序相关性           |
| post RankIC   | 样本外 RankIC              |
| yearly RankIC | 分年度泛化表现             |
| RankIC std    | 稳定性                     |
| mutual IC     | 因子池内部冗余度           |

------

## 2. 探索指标

| 指标                            | 含义                         |
| ------------------------------- | ---------------------------- |
| unique expression count         | 去重后表达式数量             |
| duplicate rate                  | 重复表达式比例               |
| AST entropy                     | 表达式结构分布熵             |
| max cluster ratio               | 最大结构簇占比               |
| operator coverage               | 算子覆盖率                   |
| avg node count                  | 平均节点数                   |
| avg tree depth                  | 平均树深                     |
| accepted count by head          | 各 head 被接收的候选数量     |
| final pool contribution by head | 最终因子池中各 head 贡献比例 |

------

# 十、关键实验表格

## 表 1：收益表现对比

| 方法                  | IC   | RankIC | post RankIC | RankIC std |
| --------------------- | ---- | ------ | ----------- | ---------- |
| Single-Transformer    |      |        |             |            |
| MultiHead             |      |        |             |            |
| MultiHead + Intrinsic |      |        |             |            |

------

## 表 2：探索覆盖对比

| 方法                  | unique count | duplicate rate | AST entropy | operator coverage |
| --------------------- | ------------ | -------------- | ----------- | ----------------- |
| Single-Transformer    |              |                |             |                   |
| MultiHead             |              |                |             |                   |
| MultiHead + Intrinsic |              |                |             |                   |

------

## 表 3：各策略 head 贡献

| head    | generated count | valid count | accepted count | final pool ratio |
| ------- | --------------- | ----------- | -------------- | ---------------- |
| base    |                 |             |                |                  |
| simple  |                 |             |                |                  |
| ts      |                 |             |                |                  |
| explore |                 |             |                |                  |

------

## 表 4：表达式复杂度分析

| 方法                  | avg node count | avg depth | risky op ratio |
| --------------------- | -------------- | --------- | -------------- |
| Single-Transformer    |                |           |                |
| MultiHead             |                |           |                |
| MultiHead + Intrinsic |                |           |                |

------

# 十一、论文方法章节写法

可以这样写：

```text
本文将公式化 Alpha 因子生成建模为带语法约束的离散序列决策过程。为了提升策略对表达式前缀状态的建模能力，本文采用 TransformerSharedNet 对 token 序列进行编码，得到共享状态表示。在此基础上，本文设计多策略 head 结构，不同策略 head 基于同一状态表示输出不同动作分布，分别对应收益导向、简单表达式、时序算子偏好和结构探索四种搜索偏好。

其中，结构探索策略引入基于 AST 结构访问次数的 count-based 内在奖励。当某类表达式结构在历史生成过程中较少出现时，系统给予更高探索奖励；随着该结构出现次数增加，探索奖励逐渐衰减。该机制用于鼓励策略覆盖更多表达式结构区域，而不是长期集中于少数高频模板。

所有策略 head 生成的候选表达式最终进入统一 AlphaPool，由 AlphaPool 根据候选因子加入当前因子池后的边际收益进行筛选。本文从收益表现和探索覆盖两个层面评价该方法：收益层面关注 IC、RankIC 和样本外稳定性，探索层面关注表达式去重数量、AST 结构熵、算子覆盖率和不同 head 的候选贡献。
```

------

# 十二、实验结论写法模板

如果结果好：

```text
实验结果表明，多策略 head 结构能够提升候选表达式覆盖范围，结构新颖性内在奖励进一步提高了 AST 结构熵和去重后候选数量。在部分测试年份中，MultiHead + Intrinsic 取得了更高的样本外 RankIC，说明多策略探索机制能够在一定程度上改善 Alpha 因子生成效果。
```

如果结果一般：

```text
实验结果表明，多策略 head 和结构新颖性内在奖励能够提高候选表达式的结构多样性，表现为 unique expression count 上升、duplicate rate 下降以及 AST entropy 提高。但收益指标并未稳定提升，说明表达式空间覆盖增强并不必然转化为更高的样本外 RankIC。
```

如果结果不好：

```text
实验结果表明，在当前实验设置下，多策略 head 和结构新颖性内在奖励未能稳定提升样本外 RankIC。尽管探索相关指标有所变化，但最终收益表现仍主要受 AlphaPool 筛选和 RE 主目标影响。这说明在 Alpha 因子生成任务中，探索增强与有效因子发现之间仍存在差距，后续需要进一步研究更合理的策略调度和内在奖励定义。
```

------

# 十三、最终贡献表述

论文贡献可以写成：

```text
本文主要工作如下：

（1）基于开源 AlphaGen 项目复现并适配了公式化 Alpha 因子强化学习生成流程，完成数据准备、训练配置、因子池评估和后处理分析。

（2）设计共享 Transformer 与多策略 head 结构，使模型能够在统一状态表征下形成收益导向、简单表达式、时序算子偏好和结构探索等不同候选生成策略。

（3）针对结构探索策略，引入基于 AST 结构访问次数的 count-based 内在奖励机制，用于鼓励模型探索较少访问的表达式结构。

（4）构建多角度实验分析流程，从 IC、RankIC、样本外稳定性、结构熵、表达式复杂度和策略 head 贡献等角度评估多策略与内在奖励机制的作用。
```

------

# 十四、最终一句话

你的最终方案就是：

> **在 AlphaGen 表达式生成框架上，使用共享 Transformer 编码表达式状态，并接入 base、simple、ts、explore 四个策略 head；其中 explore head 使用基于 AST 访问次数的 count-based 内在奖励。所有 head 生成的候选因子统一进入 AlphaPool 筛选，实验同时评估收益表现和探索覆盖。**